all: %.o

%.o: %.c
	gcc $< -o $*


#.PHONY:clean

#all: $(MAIN)

#.c.o: gcc -c $<

#$(MAIN).o: $(MAIN).c
#	gcc -c $(MAIN).c -o $(MAIN).o

#$(MAIN): $(MAIN).o
#	gcc $(MAIN).o -o $(MAIN)

#clean: 
#	rm $(MAIN) $(MAIN).o
